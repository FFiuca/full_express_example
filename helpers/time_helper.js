// it is done by cross-env TZ='Asia/Jakarta'. look package.json
// exports.date_tz = ()=>{
//     const d = new Date().toString()

//     return new Date(`${d.getFullYear()}-${d.getMonth()+1}-${d.getDay()} ${d.getHours()}:${d.getMinutes()}:${d.getSeconds()}.${d.getMilliseconds()}`)
// }

// console.log(this.date_tz())
